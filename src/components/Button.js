import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

const Button = () => (
        <TouchableOpacity>
            <Text>Click me!!</Text>
        </TouchableOpacity>
    );

export default Button;
